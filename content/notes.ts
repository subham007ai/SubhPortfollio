export type Note = {
  slug: string;
  title: string;
  date: string;
  dek: string;
};

export const notes: Note[] = [
  // Empty for now — the /notes page handles the empty state gracefully.
  // Add new entries here as Subham publishes.
];
